Implement the smallest viable module system described in docs/module-system.md: multi-file applications where each .ml file is an implicit module, top-level definitions are internally qualified as Module.name, cross-file references use qualified names, all files compile together into one COR24 image, and full OCaml features such as .mli, open, nested modules, functors, separate compilation, aliases, and visibility are explicitly deferred. The saga must include implementation, focused regression tests, user-facing demos, and documentation updates.
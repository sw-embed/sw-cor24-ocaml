Added List.map/filter/fold_left/iter as builtins with curried application. Partial state encoded in the builtin-marker closure's ival field (stage counter 0/1/2) and head/tail (stashed fn/acc). apply_val helper invokes user closures; builtin closures as HOF callbacks are unsupported (wrap in fun). New pool_put helper cuts intern boilerplate for the 4 HOF names. 40 reg-rs tests (4 lex_* still failing on upstream p24p issue #20, not related). Unlocks canonical List.map / List.filter / partial fold demos.
Implement GitHub issue #8: useful List combinators for Tuplet compiler passes. Add List.map, List.fold_left, and List.filter at minimum; add List.iter and List.find_opt if they fit cleanly. Prefer implementation style consistent with current built-in List functions and partial application behavior. Include tests for normal lists, empty lists, closures capturing environment, predicate/filter behavior, fold accumulator order, and Option-returning find_opt if implemented. Add reg-rs baselines and documentation updates. Close issue #8 after validation passes.
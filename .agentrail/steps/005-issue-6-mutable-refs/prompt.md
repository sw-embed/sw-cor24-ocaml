Implement GitHub issue #6: mutable references with ref, prefix ! dereference, and := assignment. Tuplet needs mutable registries, symbol tables, and environments without state-threading every parser/checker function. Design the smallest runtime representation for 'a ref in the current interpreter, add parser support for ! and :=, implement ref built-in behavior, and include tests for integer refs, list refs, closure capture of refs, sequencing with assignment, and failure modes. Update docs and reg-rs baselines. Close issue #6 after validation passes.
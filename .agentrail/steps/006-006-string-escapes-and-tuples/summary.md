Implemented issue #4. run-ocaml.sh now preserves literal backslashes before cor24-run UART escape decoding, allowing the existing lexer to receive and decode \n, \t, \, and ". Tuple literals and patterns of arity >= 3 now desugar to right-nested pairs, preserving existing pair value and pattern semantics. Added eval_string_escapes and eval_tuple_arity regressions. Validation: focused string/tuple tests pass; just test passes with 71 tests.
Build tests/demo_adventure.ml: an idiomatic OCaml text-adventure demonstrating ADTs and pattern matching. Scope: 3-4 rooms modeled as a variant type (type room = Cave | Hall | Garden | Exit), a player state record-or-tuple carrying current room and inventory list, and a command parser that pattern-matches against read_line results to handle n/s/e/w/look/inventory/quit. World state threaded through the loop (no refs). Use the Interactive loops patterns from docs/stdin-and-getc.md. Reminder: string (=)/(<>) work; lex ordering on strings, Char ops, and String.length do NOT — if you need them, stop and surface rather than expanding scope. Add OCAML_STDIN-fed regression test under work/reg-rs/ driving a canned walkthrough (e.g. 'n\nlook\ne\nquit\n'), a demo-adventure justfile target, and a short section in docs/stdin-and-getc.md (or new docs/text-adventure.md) explaining how the variant/match structure gives OCaml its leverage for this class of program.
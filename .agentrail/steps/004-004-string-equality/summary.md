Added VK_STRING fast path for OP_EQ and OP_NEQ in eval_expr (src/ocaml.pas) — walks string_pool byte-for-byte over (noff,nlen) windows, length mismatch short-circuits, OP_NEQ inverts. Unblocks step 003's 'if line = "quit"' pattern. Regression test tests/eval_string_eq.ml covers equal, unequal-same-length, unequal-different-length, empty-vs-empty, and both (=) and (<>). Lexicographic <,>,<=,>= intentionally out of scope.
Implement GitHub issue #7: record types and field access for Tuplet AST nodes. Support type t = { f1 : T1; f2 : T2 }, record construction { f1 = e1; f2 = e2 }, and field access r.f1. Add record pattern matching or let-destructuring if it is small; otherwise document it as deferred. Functional update { r with f = e } is nice-to-have and should be skipped unless the core record path is already solid. Add tests for construction, field access, multiple records with overlapping field names if supported, nested records, and readable failures. Add docs/reg-rs baselines. Close issue #7 after validation passes.
BLOCKED: demo_echo_loop.ml requires string equality to match the 'quit' exit condition, but (=) on VK_STRING compares ival (always 0) and returns true for any string pair. No existing primitive (String.length, int_of_string, putc/getc, pattern matching) can reconstruct string equality without char-indexing or a new primitive. Per scope discipline ('no new primitives -- if you find you need one (e.g. string equality), stop and surface it'), stopping. Recommend next step: add string equality (either a dedicated string_eq : string -> string -> bool primitive, or extend OP_EQ/OP_NEQ in eval_expr to compare VK_STRING byte-wise via string_pool). After that lands, retry this step -- demo_guess.ml does not need string equality and can be built as part of the retry.
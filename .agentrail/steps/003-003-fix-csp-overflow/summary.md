Blocked on upstream. Identified exact fix in `vendor/sw-pcode/v0.1.0/bin/pvm.s`: add `csp < eval_stack` bounds checks (trap code 2) to op_call, op_calln, op_enter, and the xcall handler; grow call_stack from 1536 → 4096 bytes. Verified locally — fib 1..11 all return correct values (was: fib 8 EVAL_ERROR, fib 9..12 TRAP 4), deep recursion now produces clean TRAP 2. All 58 existing regression tests still pass + new canonical_fib test passes. But vendor/ is gitignored (fetched from upstream by scripts/vendor-fetch.sh), so the fix cannot be committed downstream. Filed https://github.com/sw-embed/sw-cor24-pcode/issues/14 with full diff, evidence table, and patch-ready edits. Reverted local pvm.s edits via vendor-fetch to leave tree clean. Next downstream action (new step): once upstream merges, bump commit pin in vendor/sw-pcode/v0.1.0/version.json, re-fetch, re-add canonical_fib regression test, re-verify.

Added 15 canonical_*.ml + .rgt tests covering one-liner OCaml demos from docs/possible-demos.txt: arith, string concat, let-in, lambda app, fact, match-int, when-guard, List.{map,filter,fold_left}, pattern-arg let (swap), print+string_of_int+List.length, Some, safe_div. fib 10 excluded — TRAP 4 (INVALID_OPCODE) at runtime even with 1B instruction budget; fib 8 works. Pre-existing 4 lex_* failures unchanged.
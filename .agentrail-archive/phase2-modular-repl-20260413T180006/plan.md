Modularize the OCaml interpreter into separate Pascal compilation units to reduce memory pressure and enable interactive input. Split the monolithic ocaml.pas into lexer, parser, and evaluator units compiled separately. Add getc for interactive UART input using the SNOBOL4 REPL pattern (source via -u, runtime input via --terminal after EOT). Target demo: interactive REPL that reads OCaml expressions and evaluates them.
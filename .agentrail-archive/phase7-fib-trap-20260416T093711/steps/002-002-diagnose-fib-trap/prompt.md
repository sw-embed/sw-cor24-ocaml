Diagnose the fib trap. Step 001 established: fib 9 hits a clean EVAL ERROR; fib 10/11/12 all TRAP 4 (INVALID_OPCODE) at identical instruction count 3,415,640 (deterministic state-corruption, independent of N). A 150-deep let-chain runs cleanly so generic heap/env are fine — the failure is on the recursive-call path. Goal: pin down which allocation on the recursive-call path corrupts state without checking for exhaustion. Suggested approach: (1) read src/ocaml.pas to find the EVAL ERROR sites and identify which one fib 9 hits — that's the 'good' path with a check. (2) For fib 10, find the failing alloc by adding temporary instrumentation (UART prints of pointer/limit values at activation-record alloc, closure alloc, value-push) and re-running; identify which alloc bumps a pointer past its limit without a guard. (3) Examine the .p24m memory layout (build/ocaml.spc, build/ocaml_linked.spc) to see what gets corrupted when that alloc overflows — likely the code segment, which explains TRAP 4. Deliverable: append a 'Diagnosis' section to docs/fib-trap-investigation.md naming the specific alloc path, the specific missing guard, and a 1-2 line sketch of the fix (do NOT implement it). Hard constraint: src/ocaml.pas is near the ~65KB Pascal-source limit; whatever fix step 003 makes must be compact. Out of scope: the actual fix, GC, tail-call elimination.
Implemented user-defined variant constructors with single payload support. Type declarations now record constructor arity, payload constructors evaluate through the existing constructor-closure path, and PK_CTOR patterns bind payloads through try_match. Added eval_user_variants coverage for token-style variants, payload matching, nullary constructors, constructor printing, and multiple type declarations. Validation: just build passes; focused variant tests pass; just test passes with 69 tests.
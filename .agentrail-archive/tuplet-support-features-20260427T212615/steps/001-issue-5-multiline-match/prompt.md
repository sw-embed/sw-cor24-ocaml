Implement GitHub issue #5: multi-line match expressions. Tuplet lexer/parser/checker code needs match arms spread across physical lines, e.g. let dump_tok t = match t with newline | TIdent s -> ... newline | TInt n -> .... Investigate the current line-oriented REPL/source loading model and add the smallest support that lets match ... with arms span lines without forcing all arms onto one physical line. Include focused tests/reg-rs baselines for multi-line match arms, nested expressions in arm bodies where practical, and backward compatibility for one-line matches. Update docs if the source loading or multi-line form has user-visible constraints. Close issue #5 only after validation passes.
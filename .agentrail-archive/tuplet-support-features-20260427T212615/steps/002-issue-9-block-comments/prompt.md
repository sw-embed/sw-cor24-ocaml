Implement GitHub issue #9: standard OCaml block comments (* ... *) including nesting. Tuplet source needs inline comments around lexer byte handling and grammar decisions. Confirm whether existing comment skipping applies only within a single source line, then make comments work in normal source files and across the forms supported by the interpreter. Add focused tests for simple comments, inline comments, comments before declarations, nested comments, and comments that should not affect string literals. Add reg-rs baselines and documentation updates. Close issue #9 after validation passes.
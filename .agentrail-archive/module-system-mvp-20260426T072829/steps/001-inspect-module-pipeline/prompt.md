Read docs/module-system.md and the current compiler/runtime scripts to turn the MVP into an implementation contract. Confirm existing parser, evaluator/codegen, run-ocaml/build entry points, and reg-rs conventions. Write down the accepted MVP semantics in the step summary: each .ml file is an implicit module named from the filename, top-level names are internally qualified as Module.name, cross-file references require Module.name, unqualified references resolve in the current module plus builtins, all files compile together into one COR24 image, and .mli/open/nested modules/functors/separate compilation/aliases/visibility are deferred.
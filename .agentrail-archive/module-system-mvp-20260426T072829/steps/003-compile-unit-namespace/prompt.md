Add compile-unit/module identity to the compiler pipeline. Derive module names from source filenames using the MVP rule math.ml -> Math, main.ml -> Main. Internally qualify top-level definitions as Module.name and ensure same-named top-level definitions in different files do not collide. Keep the implementation scoped to whole-program compilation; do not introduce object files or .mli handling.
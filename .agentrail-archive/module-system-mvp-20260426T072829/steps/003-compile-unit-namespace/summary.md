Added the compile-unit namespace substrate for the module MVP. The interpreter now recognizes a reserved driver directive, let __module = "Name", strips unqualified top-level aliases when switching modules, and stores normal top-level definitions under both Module.name and a temporary local alias while that module is active. Function closures capture the active module aliases, so same-module helper calls continue to work after another module becomes active. Qualified Module.name references resolve through the persistent top-level environment, while unqualified cross-module access fails after a module switch. Added eval_module_namespace_directive regression. Increased scripts/build.sh default Pascal compiler instruction cap to 3000000000 because the larger interpreter now exceeds the old default but compiles successfully. Validation: just build passed; focused reg-rs tests eval_module_namespace_directive, eval_qualified_builtin, and eval_unsupported_nested_module passed. Code/test commit: aff125e.
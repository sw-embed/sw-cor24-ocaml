Teach the run/build path to accept multiple .ml inputs and compile them as one program. Parse all files, collect qualified top-level symbols, resolve Module.name references across compile units, preserve deterministic top-level evaluation/link order, and emit assembler-safe mangled names such as Module__value or Module_value. Main-file behavior for a single input must remain backward compatible.
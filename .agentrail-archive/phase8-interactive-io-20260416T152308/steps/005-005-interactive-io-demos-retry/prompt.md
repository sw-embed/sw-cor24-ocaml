Retry of aborted step 003 now that string equality works. Build the two smoke-demo programs that exercise getc + read_line end-to-end: (1) tests/demo_echo_loop.ml — a read_line / print_endline loop that exits on input 'quit' (introduces match on read_line results, or equivalently an if-else on (=)), (2) tests/demo_guess.ml — a guess-the-number game using int_of_string, reading guesses via read_line until the player guesses the fixed target (pick 42 for simplicity). For each: OCAML_STDIN-fed regression test under work/reg-rs/, demo target in justfile, short doc note in docs/stdin-and-getc.md (or new docs/interactive-demos.md) explaining the patterns. String (=) and (<>) are available as of step 004 — lexicographic ordering on strings is NOT. If you find you need something else primitive (e.g. Char ops, regex), stop and surface it rather than expanding scope. Text adventure is the *next* saga step, not this one.
Built two interactive smoke demos (demo_echo_loop sentinel-quit loop, demo_guess int_of_string + compare w/ target 42) as recursive fun u->... closures; added OCAML_STDIN-fed .rgt regression tests (both pass), demo-echo-loop and demo-guess justfile targets, and an Interactive loops section in docs/stdin-and-getc.md documenting the patterns + what's still missing (lex-ord on strings, char ops, graceful int_of_string).
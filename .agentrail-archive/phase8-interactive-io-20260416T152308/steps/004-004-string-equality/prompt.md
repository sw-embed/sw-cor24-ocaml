The step 003 demo work is blocked because OCaml's (=) and (<>) don't work on strings in this interpreter — right now the generic int-based comparison in eval_expr compares lv^.ival and rv^.ival, which for VK_STRING is always 0, so every string compares equal. Fix that here. Scope: in src/ocaml.pas, extend the EK_BINOP path for OP_EQ and OP_NEQ to detect when both operands are VK_STRING and compare by actually walking string_pool byte-for-byte over the (noff,nlen) window. Keep the existing int semantics for non-string comparisons — do not change OP_LT/GT/LE/GE on strings (out of scope). Add a regression test tests/eval_string_eq.ml covering: equal strings, unequal same-length, unequal different-length, empty string == empty string, and at least one inequality-via-(<>) check. Register it under work/reg-rs/ (ml source, .rgt with the usual run-ocaml command, .out capturing expected output). No changes to read_line, getc, the runtime, or the demos — the point of this step is only to add the primitive capability so step 003 can be retried cleanly. When done, define the next step as a retry of 003 with slug 005-interactive-io-demos-retry using the same prompt text as the original 003 (copy from agentrail history or .agentrail-archive).
Build the two smoke-demo programs that exercise getc + read_line end-to-end: (1) tests/demo_echo_loop.ml — a read_line / print_endline loop that exits on input 'quit' (introduces match on read_line results), (2) tests/demo_guess.ml — a guess-the-number game using int_of_string (from step 002 era), reading guesses via read_line until the player guesses the fixed target (pick 42 for simplicity). For each: OCAML_STDIN-fed regression test under work/reg-rs/, demo target in justfile, short doc note in docs/stdin-and-getc.md (or new docs/interactive-demos.md) explaining the patterns. Scope discipline: no text adventure (step 004), no new primitives — if you find you need one (e.g. string equality), stop and surface it.